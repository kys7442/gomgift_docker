# 브랜치 병합 분석 보고서

## 개요

- **현재 브랜치**: `mobile_css_01` (배포 예정)
- **병합한 브랜치**: `priceError_01` (일반몰, 장바구니 핵심 가격 관련 로직)
- **분석 일시**: 2025-01-XX

---

## ✅ 병합 상태 확인 결과

### 전체 평가: **적용 완료** ✅

일반몰 가격 문제 수정 가이드에 명시된 핵심 로직들이 모두 적용되어 있습니다.

---

## 📋 Phase별 적용 상태 상세 분석

### Phase 1: 일반몰용 AOV 가격 계산 함수 생성 ✅

#### 파일: `lib/aov_price_calculator_general.php`

**상태**: ✅ **생성 완료 및 적용됨**

**확인 내용**:
- ✅ `calculate_aov_price_for_general_mall()` 함수 구현됨
- ✅ `get_aov_qty_column_index()` 함수 구현됨
- ✅ 가이드 문서의 코드와 일치
- ✅ 수량 구간 계산 로직 정상 (col2~col8 지원)
- ✅ 범위 형식("1-9") 및 단일 숫자 형식("10+") 모두 지원

**위치**: 
```
lib/aov_price_calculator_general.php (전체 파일)
```

---

### Phase 2: 장바구니 저장 로직 수정 ✅

#### 파일: `shop/cartupdate.php`

**상태**: ✅ **적용 완료**

**확인 내용**:

**1. AOV 상품 가격 계산 로직 (692-704줄)**
```php
// ✅ 가이드 문서와 일치
if($it['it_aov_use'] == 'Y' && $selected_option) {
    include_once(G5_LIB_PATH.'/aov_price_calculator_general.php');
    $it_aov_price = calculate_aov_price_for_general_mall(
        $it_id, $selected_option, 'BASE', $ct_qty
    );
}
```

**2. AOV 상품 ct_price 저장 로직 (845-853줄)**
```php
// ✅ 가이드 문서와 일치
if($it['it_aov_use'] == 'Y') {
    if($it_aov_price > 0) {
        $ct_price = $it_aov_price;  // ✅ 실제 판매가격 저장
    } else {
        $ct_price = isset($it['it_price2']) && (int)$it['it_price2'] > 0 
                   ? $it['it_price2'] : $it['it_price'];
    }
} else {
    // 일반 상품: it_price2 사용 (판매가)
    $ct_price = $it['it_price2'];  // ✅ 가이드 문서와 일치
}
```

**3. 동일 옵션 중복 시 수량별 가격 재계산 (789-816줄)**
```php
// ✅ 가이드 문서와 일치
if($it['it_aov_use'] == 'Y' && $selected_option) {
    include_once(G5_LIB_PATH.'/aov_price_calculator_general.php');
    $new_it_aov_price = calculate_aov_price_for_general_mall(
        $it_id, $selected_option, 'BASE', $new_total_qty
    );
    
    $sql3 = " update {$g5['g5_shop_cart_table']}
                set ct_qty = '$new_total_qty',
                    it_aov_price = '$new_it_aov_price',
                    ct_price = '$new_it_aov_price'  // ✅ 수량별 가격 재계산
                where ct_id = '{$row2['ct_id']}' ";
}
```

**위치**:
- AOV 가격 계산: `shop/cartupdate.php` 692-704줄
- ct_price 저장: `shop/cartupdate.php` 845-870줄
- 중복 옵션 처리: `shop/cartupdate.php` 789-816줄

---

### Phase 3: JavaScript 가격 계산 수정 ✅

#### 파일: `js/shop.js`

**상태**: ✅ **적용 완료**

**확인 내용**:

**it_price2 우선 사용 로직 (2378-2393줄)**
```javascript
// ✅ 가이드 문서와 일치
// 일반몰에서는 it_price2를 기본으로 사용
var it_price2_input = $("input#it_price2").val();
var it_price = parseInt(it_price2_input) || 0;

// it_price2가 없으면 it_price 사용 (폴백)
if(isNaN(it_price) || it_price <= 0) {
    var it_price_input = $("input#it_price").val();
    it_price = parseInt(it_price_input) || 0;
}
```

**위치**: `js/shop.js` 2378-2393줄

**참고**: AOV 상품 가격 계산은 `calculateAovPriceAsync()` 함수에서 처리되며, 이는 별도로 구현되어 있습니다.

---

### Phase 4: 장바구니 수량 변경 시 AOV 가격 재계산 추가 ✅

#### 파일: `shop/ajax_cart_qty_update.php`

**상태**: ✅ **생성 완료 및 적용됨**

**확인 내용**:
- ✅ 파일 생성됨
- ✅ AOV 상품 수량별 가격 재계산 로직 구현됨
- ✅ 일반 상품과 AOV 상품 구분 처리됨
- ✅ JSON 응답 형식 정상
- ✅ 가이드 문서의 코드와 일치

**주요 로직 (26-76줄)**:
```php
// ✅ 가이드 문서와 일치
if ($cart_item['it_aov_use'] == 'Y') {
    include_once(G5_LIB_PATH.'/aov_price_calculator_general.php');
    
    // 필수옵션 추출
    $selected_option = ...;
    
    // 수량별 가격 재계산
    $new_it_aov_price = calculate_aov_price_for_general_mall(
        $cart_item['it_id'], $selected_option, 'BASE', $new_qty
    );
    
    // 장바구니 업데이트
    UPDATE ... SET ct_qty = '$new_qty',
                   it_aov_price = '$new_it_aov_price',
                   ct_price = '$new_it_aov_price'
}
```

**위치**: `shop/ajax_cart_qty_update.php` (전체 파일)

**참고**: JavaScript에서 이 엔드포인트를 호출하는 로직은 별도로 구현되어야 합니다. (가이드 문서 Phase 4-2 참조)

---

### Phase 5: 장바구니 표시 로직 일관성 확보 ✅

#### 파일: `shop/cart.php`

**상태**: ✅ **적용 완료**

**확인 내용**:

**AOV 상품 가격 표시 로직 (129-146줄)**
```php
// ✅ 가이드 문서와 일치
if($aov_row['it_aov_use'] == 'Y') {
    // AOV 상품: it_aov_price 사용 (수량별 계산된 가격)
    if($aov_row['io_type'] == 1) {
        $sell_price_aov = $aov_row['io_price'] * $aov_row['ct_qty'];
    } else {
        $base_price = isset($aov_row['it_aov_price']) && $aov_row['it_aov_price'] > 0 
                     ? $aov_row['it_aov_price'] 
                     : $aov_row['ct_price']; // 폴백
        $sell_price_aov = ($base_price + $aov_row['io_price']) * $aov_row['ct_qty'];
    }
} else {
    // 일반 상품: ct_price 사용
    $sell_price_aov = ($aov_row['ct_price'] + $aov_row['io_price']) * $aov_row['ct_qty'];
}
```

**위치**: `shop/cart.php` 129-146줄

---

## 📊 변경된 파일 목록

### 신규 생성 파일

1. **`lib/aov_price_calculator_general.php`**
   - 일반몰용 AOV 가격 계산 함수
   - Phase 1에서 생성

2. **`shop/ajax_cart_qty_update.php`**
   - 장바구니 수량 변경 AJAX 엔드포인트
   - Phase 4에서 생성

### 수정된 파일

1. **`shop/cartupdate.php`**
   - AOV 상품 가격 계산 로직 추가 (692-704줄)
   - ct_price 저장 로직 수정 (845-870줄)
   - 동일 옵션 중복 시 수량별 가격 재계산 (789-816줄)
   - Phase 2에서 수정

2. **`shop/cart.php`**
   - AOV 상품 가격 표시 로직 수정 (129-146줄)
   - JavaScript 수량 변경 이벤트 처리 추가 (946-1002줄)
   - Phase 4, Phase 5에서 수정

3. **`js/shop.js`**
   - it_price2 우선 사용 로직 수정 (2378-2393줄)
   - Phase 3에서 수정

---

## ⚠️ 주의사항 및 확인 필요 사항

### 1. JavaScript 수량 변경 이벤트 처리

**상태**: ✅ **적용 완료**

가이드 문서 Phase 4-2에 명시된 JavaScript 수량 변경 이벤트 처리가 이미 구현되어 있습니다.

**확인 내용**:
- ✅ `shop/cart.php` 946-1002줄에 구현됨
- ✅ `input[name^="ct_qty"]` change 이벤트 핸들러 존재
- ✅ `ajax_cart_qty_update.php` 호출 로직 구현됨
- ✅ AOV 상품 감지 및 처리 로직 포함
- ✅ 단가 및 소계 업데이트 로직 포함
- ✅ 총 금액 재계산 로직 포함

**구현 위치**: `shop/cart.php` 946-1002줄

**주요 로직**:
```javascript
// ✅ 가이드 문서와 일치
$(document).on('change', 'input[name^=ct_qty]', function() {
    var $input = $(this);
    var ct_id = ...;
    var new_qty = parseInt($input.val()) || 1;
    
    // AOV 상품인지 확인
    var is_aov = ...;
    
    if (is_aov && ct_id) {
        // AOV 상품: 서버에서 수량별 가격 재계산
        $.ajax({
            url: G5_SHOP_URL + '/ajax_cart_qty_update.php',
            type: 'POST',
            dataType: 'json',
            data: { ct_id: ct_id, qty: new_qty },
            success: function(response) {
                // 단가 및 소계 업데이트
                // 총 금액 재계산
            }
        });
    }
});
```

### 2. 폐쇄몰과의 분리 확인

**상태**: ✅ **적용됨**

- `shop/cartupdate.php`: 일반몰만 처리 (폐쇄몰은 `closedmall_cartupdate.php` 사용)
- `shop/ajax_cart_qty_update.php`: `ct_closed_mall_id IS NULL OR ct_closed_mall_id = 0` 조건 확인됨
- `shop/cart.php`: 일반몰 장바구니만 처리

### 3. 기존 데이터 호환성

**상태**: ✅ **호환됨**

- 기존 장바구니 데이터는 수정하지 않음
- 새로운 장바구니 추가 시에만 수정된 로직 적용
- 폴백 로직으로 기존 데이터 처리 가능

---

## 🧪 테스트 권장 사항

### 시나리오 1: AOV 상품 장바구니 추가
1. AOV 상품 상세 페이지 접속
2. 필수옵션 선택
3. 수량 1개로 장바구니 추가
4. **검증**: DB에서 `ct_price`와 `it_aov_price` 값이 일치하는지 확인
5. **검증**: `ct_price`에 매입가가 아닌 실제 판매가격이 저장되었는지 확인

### 시나리오 2: AOV 상품 수량별 가격 확인
1. AOV 상품 상세 페이지 접속
2. 필수옵션 선택
3. 수량 1개로 장바구니 추가 → 가격 확인
4. 수량 10개로 변경 → 가격 확인 (가격이 달라져야 함)
5. 수량 50개로 변경 → 가격 확인 (가격이 달라져야 함)

### 시나리오 3: 일반 상품 장바구니 추가
1. 일반 상품 상세 페이지 접속
2. 옵션 선택 (또는 옵션 없음)
3. 장바구니 추가
4. **검증**: DB에서 `ct_price`에 `it_price2` 값이 저장되었는지 확인
5. **검증**: `it_aov_price`는 0인지 확인

### 시나리오 4: 장바구니 수량 변경
1. 장바구니 페이지 접속
2. AOV 상품 수량 변경 (1개 → 10개)
3. **검증**: 가격이 재계산되어 표시되는지 확인
4. **검증**: DB에서 `ct_price`와 `it_aov_price` 값이 업데이트되었는지 확인

### 시나리오 5: JavaScript 가격 계산
1. 일반 상품 상세 페이지 접속
2. 옵션 선택
3. 수량 변경
4. **검증**: 총액이 `it_price2` 기반으로 계산되는지 확인

---

## 📝 결론

### ✅ 적용 완료 항목

1. ✅ Phase 1: 일반몰용 AOV 가격 계산 함수 생성
2. ✅ Phase 2: 장바구니 저장 로직 수정
3. ✅ Phase 3: JavaScript 가격 계산 수정
4. ✅ Phase 4: 장바구니 수량 변경 시 AOV 가격 재계산 추가
   - ✅ 서버 측: `shop/ajax_cart_qty_update.php`
   - ✅ 클라이언트 측: `shop/cart.php` JavaScript 이벤트 처리
5. ✅ Phase 5: 장바구니 표시 로직 일관성 확보

### ⚠️ 확인 필요 항목

없음 (모든 항목 적용 완료)

### 📌 권장 사항

1. ✅ **JavaScript 수량 변경 이벤트**: `shop/cart.php`에 이미 구현되어 있음
2. **통합 테스트**: 위의 테스트 시나리오를 모두 실행하여 정상 동작 확인
3. **폐쇄몰 분리 확인**: 일반몰 수정이 폐쇄몰에 영향을 주지 않는지 최종 확인
4. **배포 전 검증**: 모든 Phase가 적용되었으므로 통합 테스트 후 배포 권장

---

## 📍 변경 파일 위치 요약

```
신규 생성:
├── lib/aov_price_calculator_general.php (전체)
└── shop/ajax_cart_qty_update.php (전체)

수정:
├── shop/cartupdate.php
│   ├── 692-704줄: AOV 가격 계산 로직
│   ├── 789-816줄: 동일 옵션 중복 시 수량별 가격 재계산
│   └── 845-870줄: ct_price 저장 로직
├── shop/cart.php
│   ├── 129-146줄: AOV 상품 가격 표시 로직
│   └── 946-1002줄: JavaScript 수량 변경 이벤트 처리
└── js/shop.js
    └── 2378-2393줄: it_price2 우선 사용 로직
```

---

**작성일**: 2025-01-XX  
**분석자**: AI Assistant  
**검토 필요**: JavaScript 수량 변경 이벤트 처리 확인
